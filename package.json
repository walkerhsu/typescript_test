{
  "name": "test_vite_proj",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pwd && vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server_1": "cd backend && ts-node server.ts",
    "server": "cd backend && nodemon"
  }
}
